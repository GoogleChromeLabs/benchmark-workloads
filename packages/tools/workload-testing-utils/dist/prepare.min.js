const fs=require("fs").promises,{resolve:resolve}=require("path");async function updateVariables({meta:e,src:a}){let r=await fs.readFile(`${a}`,"utf8");if(e){const a=await fs.readFile(resolve(e)),{name:s,version:p}=JSON.parse(a),i=new RegExp("appName = .*");r=r.replace(i,`appName = "${s}";`);const t=new RegExp("appVersion = .*");r=r.replace(t,`appVersion = "${p}";`)}await fs.writeFile(`${a}`,r)}async function prepare(){await updateVariables({meta:"./package.json",src:"src/workload-test.mjs"}),console.log("Done with preparation!")}prepare();